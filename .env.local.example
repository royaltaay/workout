# Supabase — get these from your project dashboard:
# https://supabase.com/dashboard → Project Settings → API Keys
# Use the "Publishable key" (NOT the Secret key)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_your-key-here

# Supabase service role — needed for webhook handler (bypasses RLS)
# https://supabase.com/dashboard → Project Settings → API Keys → service_role
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Stripe — https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_ID=price_...

# App URL — used for Stripe checkout redirect
NEXT_PUBLIC_APP_URL=http://localhost:3000
